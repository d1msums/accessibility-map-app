# Optional. Enables AI photo verification (Gemini). Without it a basic image check runs.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
# Optional. Persist state between restarts (default: fresh seed every start).
KB_PERSIST=0
PORT=3000

// Person 4: calls the Claude vision API to check the photo matches the claimed type tag.
// Input: { photoUrl, claimedType } -> Output: { verified: boolean, confidence: number }
export default async function verifyPhoto() {
  // TODO: call Claude API, feed result into reports.ai_verified / ai_confidence
}
